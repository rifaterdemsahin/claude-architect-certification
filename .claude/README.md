# ⚙️ .claude — Local Kilo/AI Agent Configuration

> **Purpose:** Local configuration for Kilo and AI agent tools — project-level settings, permissions, and overrides.

## Files

| File | Description |
|------|-------------|
| `settings.local.json` | Local-only agent settings overrides (not committed to template) |

## Rules
- This is the Kilo config directory — do not delete
- `settings.local.json` contains user-specific agent preferences